# Experimental tauri packaging

```sh
cd src-tauri
npm run dev
npm run build # supposed to build but game assets too big or something and gets stuck at the end I need to fix that

# run if you're getting symbol related errors
cargo clean
sudo ldconfig
```
